#!/bin/bash
#
# Automount DDEV Configuration
# Copy this file to config.sh and edit to customize paths for your setup
#

# Mount point for sites (must be configured in /etc/fstab)
MOUNT_POINT="/path/to/mount"

# Remote server hostname to ping
REMOTE_HOST="your-server-hostname"
